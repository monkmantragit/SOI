# Environment
NODE_ENV="development"  # or "production"

# Application URLs
NEXTAUTH_URL="http://localhost:3000"  # Base URL of your application
NEXT_PUBLIC_DOMAIN="http://localhost:3000"  # Public domain for client-side
ALLOWED_ORIGINS="http://localhost:3000"  # CORS allowed origins (comma-separated for multiple)

# Database Configuration
DATABASE_URL="postgresql://username:password@host:port/database"  # Your PostgreSQL connection string

# Directus (headless CMS)
NEXT_PUBLIC_DIRECTUS_URL="https://your-directus-host"       # Public Directus base URL
NEXT_PUBLIC_DIRECTUS_TOKEN="your_public_read_token"         # Public (read-only) token, safe to expose
DIRECTUS_ADMIN_TOKEN="your_admin_token"                     # Server-only admin token (used by npm run seo:migrate)

# SEO / ISR revalidation
# Long random string; the /api/revalidate endpoint requires it. Point a Directus
# Flow webhook at POST https://sportsorthopedics.in/api/revalidate?secret=THIS
REVALIDATE_SECRET="your_long_random_string_here"
# Optional: override the default 300s Directus cache TTL used across the app.
# DIRECTUS_REVALIDATE_SECONDS="300"

# Authentication Secrets
# Generate these using: `openssl rand -base64 32`
JWT_SECRET="your_jwt_secret_here"  # Secret for JWT tokens
NEXTAUTH_SECRET="your_nextauth_secret_here"  # Secret for NextAuth.js

# Admin Account
ADMIN_EMAIL="your_admin_email@example.com"  # Admin login email
# Generate this using bcrypt (cost factor 10)
ADMIN_PASSWORD="$2b$10$YourHashedPasswordHere"  # Hashed admin password

# Optional: Email Configuration (if you want to add email notifications)
# SMTP_HOST=""
# SMTP_PORT=""
# SMTP_USER=""
# SMTP_PASSWORD=""
# SMTP_FROM=""

# Optional: External Services
# GOOGLE_CALENDAR_ID=""
# GOOGLE_CLIENT_ID=""
# GOOGLE_CLIENT_SECRET="" 

BOOKING_WEBHOOK_URL="https://primary-14xd-production.up.railway.app/webhook/ed002bd7-c14a-46dd-a141-a23b8f8281c7"